
export function evaluateParabola(a: Array<number>, x: number): number
// requires: true
// effects: if a.length == 3 returns y coordinate of parabola a[2]x^2 + a[1]x + a[0] evaluated at x. if a.length != 3 throws exception
{ throw new Error("not implementing today"); }



export function factor(n: bigint): { p: bigint, q: bigint }
// requires: n > 0
// effects: returns p, q such that n = p*q and p is prime
{ throw new Error("not implementing today"); }



export function deleteAllOccurrences(a: Array<number>, value: number): void
// requires: true
// effects: modifies a such that a does not contain value
{ throw new Error("not implementing today"); }



export function split(s: string, separator: string): Array<string>
// requires: separator.length = 1
// effects: splits string s into array using separator as delimiter
{ throw new Error("not implementing today"); }
